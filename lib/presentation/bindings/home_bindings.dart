import 'package:get/get.dart';

/// Bindings for home page dependencies
class HomeBindings extends Bindings {
  @override
  void dependencies() {
    // Add home page specific bindings here
    // Example:
    // Get.lazyPut<HomeController>(() => HomeController());
  }
}
